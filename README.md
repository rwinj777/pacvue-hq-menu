
  # Functional Navigation Prototype

  This is a code bundle for Functional Navigation Prototype. The original project is available at https://www.figma.com/design/xUshMblAEUsOAkVCOpqyYb/Functional-Navigation-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  